{
  "name": "tiongtech-app",
  "version": "1.0.0",
  "private": true,
  "description": "TIONGTECH ERP — build and test tooling. The app itself is app.js, loaded directly by index.html.",
  "scripts": {
    "build": "node tests/build.mjs app.jsx app.js",
    "test": "node tests/test-payroll.mjs app.js"
  },
  "devDependencies": {
    "@babel/standalone": "^7.29.7"
  }
}
